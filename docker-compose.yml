services:
  api_gateway:
    build: ./api_gateway
    ports:
      - "8760:5000"
    depends_on:
      - data_integrator
      - computations
    networks:
      - net-skyplot

  data_integrator:
    build: ./data_integrator
    ports:
      - "8761:5001"
    networks:
      - net-skyplot

  computations:
    build: ./computations
    ports:
      - "8762:5002"
    depends_on:
      - data_integrator
    networks:
      - net-skyplot

networks:
  net-skyplot:
    driver: bridge
